# MongoDB connection string for local dev or Render
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# JWT secret for auth
JWT_SECRET=change_this_secret

# Application port
PORT=3000
